ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Absolute path to the extracted KuaiRand-Pure CSVs (the directory that holds
# log_standard_4_08_to_4_21_pure.csv etc). The CSVs are ~200 MB and are NOT
# vendored into this repo -- they stay in the starter kit checkout. See
# harness/VENDORED.md.
KUAIRAND_PATH=/Users/yuzherng/Downloads/kuairand-starter-kit/KuaiRand-Pure/data

# Model the real CodingAgent (agent/coding/) uses to write train.py.
CODING_AGENT_MODEL=gpt-5
