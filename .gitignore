KuaiRand-Pure/
data/
*.pt
*.ckpt
*.parquet
runs/
outputs/
__pycache__/
*.py[cod]
.venv/
venv/
.env
.ipynb_checkpoints/
.DS_Store

# Harness run artifacts (checkpoints, per-seed result.json, raw prediction
# dumps, quarantined test metrics, LLM usage log). Regenerated by every run.
logs/artifacts/
logs/quarantine/
logs/*.jsonl
logs/*.json
!logs/.gitkeep

# Solution dirs the CodingAgent generates per attempt (each one holds a copy
# of the vendored harness + a generated train.py). Reproducible from the log.
solutions/
